# --- API ---
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.9
python-multipart>=0.0.9

# --- Storage ---
SQLAlchemy>=2.0.30

# --- Vector memory ---
# ChromaDB ships an ONNX build of all-MiniLM-L6-v2, so semantic embeddings work
# with no extra download and no torch. Installing sentence-transformers is
# optional: vector_memory prefers it automatically when present, but it pulls in
# ~1 GB of torch for the same model, so it is not required here.
chromadb>=0.5.0

# --- Fallback retrieval + data handling ---
scikit-learn>=1.5
numpy>=1.26

# --- LLM (optional; the pipeline runs fully without a key) ---
anthropic>=0.40

# --- Demo UI ---
streamlit>=1.38

# --- Utilities ---
python-dotenv>=1.0
requests>=2.32

# --- Tests ---
pytest>=8.0
httpx>=0.27
